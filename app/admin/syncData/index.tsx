import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

const SyncDataPage = () => {
  return (
    <ThemedView>
      <ThemedText>Sync Data Page</ThemedText>
    </ThemedView>
  );
};

export default SyncDataPage;
