import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

const ViewDataPage = () => {
  return (
    <ThemedView>
      <ThemedText>View Data Page</ThemedText>
    </ThemedView>
  );
};

export default ViewDataPage;
