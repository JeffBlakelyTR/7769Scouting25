import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

const TeleopPage = () => {
  return (
    <ThemedView>
      <ThemedText>Teleop Page</ThemedText>
    </ThemedView>
  );
};

export default TeleopPage;
